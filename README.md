# 💻 Frontend: SOAP vs REST Comparison

Клиентская часть курсовой работы. Веб-интерфейс для визуального сравнения производительности SOAP и REST запросов.
Написано на **Vue 3 + TypeScript + Vite** с использованием **Vuetify**.

## 🚀 Как запустить с нуля

**Важно:** Для корректной работы сначала запустите **Backend**!

### 1. Установка Node.js

Если еще не установлено:
👉 [Скачать Node.js](https://nodejs.org/)

### 2. Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

```bash
npm run dev
```

### После этого в терминале появится ссылка (обычно `http://localhost:5173`). Откройте её в браузере.

---

## 📊 Функционал

Приложение разделено на две панели:

1.  **REST Service (Синяя):** Делает запросы через стандартный `fetch` и получает JSON.
2.  **SOAP Service (Оранжевая):** Формирует XML-конверты вручную, отправляет их и парсит XML-ответы через `DOMParser`.

**Метрики:**
Приложение автоматически замеряет и выводит:

- ⏳ **Duration:** Общее время выполнения запроса.
- 🧠 **Parsing Time:** Время, затраченное браузером на обработку данных (JSON vs XML).
- 📦 **Data Size:** Размер полученного ответа.

## 🛠 Стек технологий

- **Vue 3 (Composition API)** — JS фреймворк.
- **Vite** — сборщик проекта.
- **Vuetify** — библиотека UI-компонентов (Material Design).
- **TypeScript** — строгая типизация.
